/// Generic terms make no restrictions but also have inefficient implementations of most traits.
pub mod generic;
/// Ring exchange terms allow for offdiagonal matrix elements between only two pre-chosen values.
pub mod ring_exchange;
/// ZZ and X terms.
pub mod tfim;
